spring:
  profiles:
    active: local

  application:
    name: backend

  config:
    import:
      - optional:classpath:security.yml

  jpa:
    show-sql: true
    properties:
      hibernate:
        highlight_sql: true
        format_sql: true
        use_sql_comments: true

server:
  port: 8080
