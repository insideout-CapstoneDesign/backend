name: insideout-local

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env.deploy
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
